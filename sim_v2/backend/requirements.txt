# sim_v2 backend dependencies
# v1's existing requirements.txt should still cover sim/ — these are additive.

fastapi[standard]>=0.110
pydantic>=2.5
uvicorn[standard]>=0.27
websockets>=12.0

# numpy and pyarrow already required by v1 sim/
numpy>=1.26
pyarrow>=15.0

# Deployment (only needed for `modal deploy`)
modal>=0.64
